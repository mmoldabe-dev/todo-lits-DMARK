{
  "name": "todoapp-frontend",
  "private": true,
  "version": "1.0.0",
  "description": "TodoApp frontend built with Vite and Vanilla JavaScript",
  "scripts": {
    "dev": "vite --port 34115 --host",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src --ext js --report-unused-disable-directives --max-warnings 0",
    "format": "prettier --write src/"
  },
  "devDependencies": {
    "vite": "^4.4.5",
    "eslint": "^8.45.0",
    "prettier": "^3.0.0"
  },
  "browserslist": [
    "defaults",
    "not ie 11",
    "not op_mini all"
  ]
}